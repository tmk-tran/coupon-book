// Flexbox style for buttons in modals OrgDetails component
export const btnStyle = {
  display: "flex",
  flexDirection: "row",
  justifyContent: "space-between",
};

// Style for center of divs
export const centerDiv = {
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
};

// Icon Color
export const styleIconColor = { color: "#273b91" };

// Style for images in CouponList
export const styleImage = {
  width: "200px",
  height: "100px",
  objectFit: "contain",
};

// Capitalize characters in a string
export const allCaps = (sentence) => {
  return sentence.toUpperCase();
};

// Center text
export const centerText = {
  textAlign: "center",
};
